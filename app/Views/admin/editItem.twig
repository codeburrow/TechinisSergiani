{% extends "master.twig" %}

{% block content %}

    <div class="right_col" role="main">
        <div class="">
            <div class="page-title">
                <div class="title_left">
                    <h3>Edit Item
                        <small> in portfolio page</small>
                    </h3>
                </div>
            </div>

            <div class="clearfix"></div>

            <div class="row">
                <div class="col-md-12 col-sm-12 col-xs-12">
                    <div class="x_panel">
                        <div class="x_title">
                            <h2>Click on the fields to edit</h2>
                            <ul class="nav navbar-right panel_toolbox">
                                <li><a class="collapse-link"><i class="fa fa-chevron-up"></i></a>
                                </li>
                            </ul>
                            <div class="clearfix"></div>
                        </div>
                        <div class="x_content">

                            {% if result.success == 1 %}
                                <h3 style="color: forestgreen">{{ result.message }}</h3>
                            {% else %}
                                <h3 style="color: tomato">{{ result.message }}</h3>
                            {% endif %}

                            <form action="/admin/editItem" method="post">
                                <table id="datatable"
                                       class="table table-striped table-bordered responsive-utilities jambo_table">
                                    <thead>
                                    <tr>
                                        <th>ID</th>
                                        <th>URL Name</th>
                                        <th>Title</th>
                                        <th>Subtitle</th>
                                        <th>Tags</th>
                                        <th>Description</th>
                                        <th>Image</th>
                                    </tr>
                                    </thead>

                                    <tbody>
                                    {% for item in items %}
                                        <tr>
                                            <td><input type="checkbox" name="items[]" value="{{ item.id }}"/></td>
                                            <td><input style="border: 1px solid lightblue;" type="text" name="urlName[{{ item.id }}]" value="{{ item.urlName }}"></td>
                                            <td><input style="border: 1px solid lightblue;" type="text" name="title[{{ item.id }}]" value="{{ item.title }}"></td>
                                            <td><input style="border: 1px solid lightblue;" type="text" name="subtitle[{{ item.id }}]" value="{{ item.subtitle }}"></td>
                                            <td><input style="border: 1px solid lightblue;" type="text" name="tags[{{ item.id }}]" value="{{ item.tags }}"></td>
                                            <td><input style="border: 1px solid lightblue;" type="text" name="description[{{ item.id }}]" value="{{ item.description }}"></td>
                                            <td><img style="max-height: 150px; max-width: 150px;"
                                                     src="/images/{{ item.image }}" alt="image"></td>
                                        </tr>
                                    {% endfor %}
                                    </tbody>
                                </table>

                                <div class="form-group-lg" style="margin: 10px auto;">
                                    <button style="margin: 30px auto;" type="submit"
                                            class="btn btn-warning btn-block pull-left">
                                        Edit Selected Items
                                    </button>
                                </div>

                            </form>

                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>

{% endblock content %}